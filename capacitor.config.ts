import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Access Bank (Successful Transfer)',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
